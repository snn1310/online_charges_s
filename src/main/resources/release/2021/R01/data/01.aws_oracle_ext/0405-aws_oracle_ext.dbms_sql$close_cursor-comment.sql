COMMENT ON FUNCTION aws_oracle_ext.dbms_sql$close_cursor(NUMERIC)
IS
'The procedure closes a given cursor. The memory allocated to the cursor is released and one can no longer fetch from that cursor.';