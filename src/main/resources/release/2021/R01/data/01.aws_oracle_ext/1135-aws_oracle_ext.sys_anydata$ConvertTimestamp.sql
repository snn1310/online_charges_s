CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$converttimestamp(value TIMESTAMP WITHOUT TIME ZONE)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.TIMESTAMP', $1);     
$BODY$
LANGUAGE sql;