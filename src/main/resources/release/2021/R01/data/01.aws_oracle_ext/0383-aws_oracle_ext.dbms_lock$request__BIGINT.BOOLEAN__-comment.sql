COMMENT ON FUNCTION aws_oracle_ext.dbms_lock$request(BIGINT,
                                                     INTEGER,
                                                     INTEGER,
                                                     BOOLEAN)
IS
'This function requests a lock with a given mode.';