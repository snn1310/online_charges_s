CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$converttimestamptz(value TIMESTAMP WITH TIME ZONE)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.TIMESTAMP_WITH_TIMEZONE', $1);        
$BODY$
LANGUAGE sql;