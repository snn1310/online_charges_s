CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertchar(value CHARACTER) 
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.CHAR', $1);     
$BODY$
LANGUAGE sql;