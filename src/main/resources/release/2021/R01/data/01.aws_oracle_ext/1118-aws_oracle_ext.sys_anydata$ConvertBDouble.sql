CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertbdouble(value DOUBLE PRECISION)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.BINARY_DOUBLE', $1);
$BODY$
LANGUAGE sql;