CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertdate(value TIMESTAMP WITHOUT TIME ZONE) 
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.DATE', $1);  
$BODY$
LANGUAGE sql;