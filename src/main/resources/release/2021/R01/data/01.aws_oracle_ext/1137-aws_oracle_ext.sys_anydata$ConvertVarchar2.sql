CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertvarchar2(value TEXT) 
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.VARCHAR2', $1);
$BODY$
LANGUAGE sql;