COMMENT ON FUNCTION aws_oracle_ext.dbms_sql$parse(INTEGER,
                                                  TEXT,
                                                  INTEGER)
IS
'This function parses the given statement in the given cursor. DDL and DCL statements are run immediately when parsed.';