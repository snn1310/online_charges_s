CREATE OR REPLACE FUNCTION aws_oracle_ext.sessiontimezone()
RETURNS CHARACTER VARYING
AS 
$BODY$
  	
      SELECT  COALESCE(CURRENT_SETTING('TIMEZONE', TRUE), 'UTC');
      
$BODY$
STABLE
LANGUAGE SQL;