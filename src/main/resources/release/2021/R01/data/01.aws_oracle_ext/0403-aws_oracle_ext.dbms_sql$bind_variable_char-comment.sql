COMMENT ON FUNCTION aws_oracle_ext.dbms_sql$bind_variable_char(INTEGER,
                                                               TEXT,
                                                               ANYELEMENT,
                                                               INTEGER,
                                                               BOOLEAN)
IS
'This function binds a given CHAR value to a given variable in a cursor, based on the name or list of placeholders, of the variable in the statement.';