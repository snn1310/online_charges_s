CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$converttimestampltz(value TIMESTAMP WITHOUT TIME ZONE)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.TIMESTAMP_WITH_LTZ', $1);     
$BODY$
LANGUAGE sql;