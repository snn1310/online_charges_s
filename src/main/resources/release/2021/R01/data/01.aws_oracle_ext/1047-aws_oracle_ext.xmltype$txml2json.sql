create type aws_oracle_ext.xmltype$txml2json as (iNum int,
                                                 rNum bigint,
                                                 pNum bigint,
                                                 recKey text,
                                                 recValue json
                                                );