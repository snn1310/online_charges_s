CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertbfile(value CHARACTER VARYING)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.BFILE', $1);    
$BODY$
LANGUAGE sql;