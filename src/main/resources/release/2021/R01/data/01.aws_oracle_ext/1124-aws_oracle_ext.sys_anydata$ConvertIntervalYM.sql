CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertintervalym(value INTERVAL YEAR TO MONTH)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.INTERVAL_YEAR_MONTH', $1);  
$BODY$
LANGUAGE sql;