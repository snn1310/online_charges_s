CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertnchar(value CHARACTER)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.NCHAR', $1);  
$BODY$
LANGUAGE sql;