COMMENT ON FUNCTION aws_oracle_ext.utl_url$escape(TEXT,
                                                  BOOLEAN,
                                                  TEXT)
IS
'This function returns a URL with illegal characters (and optionally reserved characters) escaped using the %2-digit-hex-code format.';