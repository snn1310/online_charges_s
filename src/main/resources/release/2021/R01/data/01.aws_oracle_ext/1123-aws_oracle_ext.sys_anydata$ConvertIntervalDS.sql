CREATE OR REPLACE FUNCTION aws_oracle_ext.sys_anydata$convertintervalds(value INTERVAL DAY TO SECOND)
 RETURNS jsonb
IMMUTABLE
PARALLEL SAFE
AS
$BODY$
    SELECT JSONB_BUILD_OBJECT('SYS.INTERVAL_DAY_SECOND', $1);  
$BODY$
LANGUAGE sql;