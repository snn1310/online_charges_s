COMMENT ON FUNCTION aws_oracle_ext.regexp_substr(TEXT,
                                                 TEXT,
                                                 NUMERIC,
                                                 NUMERIC,
                                                 TEXT)
IS
'This function returns one occurrence of a substring of a string that matches the regular expression pattern. If the pattern expression is not found, the result is the null value.';
