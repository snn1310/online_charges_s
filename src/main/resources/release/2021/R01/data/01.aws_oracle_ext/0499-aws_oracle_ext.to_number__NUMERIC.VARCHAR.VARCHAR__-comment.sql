COMMENT ON FUNCTION aws_oracle_ext.to_number(NUMERIC, VARCHAR, VARCHAR) IS 'Converts NUMBER to NUMBER when additional params are set. Some format models could return incorrect result. NLS params must be valid.';
