COMMENT ON FUNCTION aws_oracle_ext.dbms_assert$enquote_name(TEXT,
                                                            BOOLEAN)
IS
'This function encloses the input string within double quotes, unless they are already present.';