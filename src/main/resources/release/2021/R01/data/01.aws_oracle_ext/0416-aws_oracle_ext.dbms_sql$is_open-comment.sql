COMMENT ON FUNCTION aws_oracle_ext.dbms_sql$is_open(INTEGER)
IS
'The function checks if the cursor with given cursor id (cursor_id) is currently opened. Returns TRUE if the cursor with given cursor id is opened.';