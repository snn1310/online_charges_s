COMMENT ON FUNCTION aws_oracle_ext.dbms_sql$column_value_char(INTEGER,
                                                              INTEGER,
                                                              ANYELEMENT,
                                                              NUMERIC,
                                                              NUMERIC,
                                                              NUMERIC)
IS
'This procedure returns the value of the cursor element for a given position in a given cursor. This procedure is used to access the data fetched by calling `dbms_sql$fetch_rows` function.';